export interface Upload {
    name: string
    title: string
    comments: string
}

export interface Archive {
    bundleId: string
    date: string
    title: string
    name: string
    comments: string
    urls: string[]
}